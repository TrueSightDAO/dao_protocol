# Server dependencies for the dao_protocol FastAPI service (the [server] half).
# Keeps the CLI install lean: CLI users install `requirements.txt` (or `pip install -e .`);
# the server box installs this file.
#
# Deploy (editable package + base + server):  pip install -e . -r requirements-server.txt
-r requirements.txt

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic-settings>=2.2.0
google-api-python-client>=2.100.0
google-auth>=2.23.0
easypost>=8.0.0
python-multipart>=0.0.6
stripe>=7.0.0
bugsnag>=4.7.0
