"""Google Sheets adapters — Python ports of the Rails ``app/models/gdrive/*``.

Built on `base.py` (a service-account Sheets v4 client, same pattern as
`dapp_digital_signature_onboarding/demo_edgar_digital_signature_sheet_flow.py`).
This is the foundation PR3+ build on (newsletter/email-agent tracking now; QR
lookup, contributors-digital-signatures, etc. later).
"""
